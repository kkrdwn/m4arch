[package]
name = "m4arch-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
m4arch-core = { path = "../m4arch-core" }
m4arch-service = { path = "../m4arch-service" }
serde_json = "1.0"

[dev-dependencies]
assert_cmd = "2.0"
predicates = "3.0"
